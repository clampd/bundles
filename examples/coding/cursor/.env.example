# Demo agent API key — copy from dashboard after enabling the coding/cursor bundle.
CLAMPD_API_KEY=ag_test_replace_me

# Your dashboard's gateway URL. Defaults to local docker dev.
CLAMPD_GATEWAY_URL=http://localhost:8080

# Demo agent ID — copy from dashboard.
CLAMPD_AGENT_ID=replace-with-uuid
